#include <stdio.h>

int main() {
    // Relational Operators 

    int n;
    scanf("%d", &n);

    // <  Check if n is greater than 10 -->
    // > Check if n is less than 10 -->
    // <= Check if n is equal to or greater than 10 -->
    // >= Check if n is equal to or less than 10 -->
    // == Check if n is equal to 10 -->
    // != Check if n is not equal to 10 -->
    

    return 0;
}